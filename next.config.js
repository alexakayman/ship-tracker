/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "github.com",
      "ghchart.rshah.org",
      "avatars.githubusercontent.com",
    ],
  },
};

module.exports = nextConfig;
